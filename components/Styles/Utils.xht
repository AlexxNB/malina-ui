<style global>
    .bg-text{background-color: var(--color-text) !important}
    .bg-bg{background-color: var(--color-bg) !important}
    .bg-primary{background-color: var(--color-primary) !important}
    .bg-secondary{background-color: var(--color-secondary) !important}
    .bg-error{background-color: var(--color-error) !important}
    .bg-success{background-color: var(--color-success) !important}
    .bg-warning{background-color: var(--color-warning) !important}
    .bg-dark{background-color: var(--color-dark) !important}
    .bg-darkest{background-color: var(--color-darkest) !important}
    .bg-neutral{background-color: var(--color-neutral) !important}
    .bg-light{background-color: var(--color-light) !important}
    .bg-lightest{background-color: var(--color-lightest) !important}
    .bg-transparent{background-color: transparent !important}

    .bd-text{border: 1px solid var(--color-text) !important}
    .bd-bg{border: 1px solid var(--color-bg) !important}
    .bd-primary{border: 1px solid var(--color-primary) !important}
    .bd-secondary{border: 1px solid var(--color-secondary) !important}
    .bd-error{border: 1px solid var(--color-error) !important}
    .bd-success{border: 1px solid var(--color-success) !important}
    .bd-warning{border: 1px solid var(--color-warning) !important}
    .bd-dark{border: 1px solid var(--color-dark) !important}
    .bd-darkest{border: 1px solid var(--color-darkest) !important}
    .bd-neutral{border: 1px solid var(--color-neutral) !important}
    .bd-light{border: 1px solid var(--color-light) !important}
    .bd-lightest{border: 1px solid var(--color-lightest) !important}
    .bd-transparent{border: 1px solid transparent !important}

    .txt-text{color: var(--color-text) !important}
    .txt-bg{color: var(--color-bg) !important}
    .txt-primary{color: var(--color-primary) !important}
    .txt-secondary{color: var(--color-secondary) !important}
    .txt-error{color: var(--color-error) !important}
    .txt-success{color: var(--color-success) !important}
    .txt-warning{color: var(--color-warning) !important}
    .txt-dark{color: var(--color-dark) !important}
    .txt-darkest{color: var(--color-darkest) !important}
    .txt-neutral{color: var(--color-neutral) !important}
    .txt-light{color: var(--color-light) !important}
    .txt-lightest{color: var(--color-lightest) !important}
    .txt-transparent{color: transparent !important}

    .radius {border-radius: var(--radius)}

    .box-shadow{box-shadow: 1px 2px 7px rgba(0,0,0,.15)}

    .mb{margin-bottom: 2rem}

    .mb-2{margin-bottom: 4rem}

    .mb-3{margin-bottom: 6rem}
</style>